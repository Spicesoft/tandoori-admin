define service {
    service_description         Check APT updates via SNMP
    use                         linux-service
    register                    0
    host_name                   apt
    check_command               check_apt_snmp
}
